#include "main.h"

/**
* main - test the function positive_or_negetive for correct outputi
* 
* Description: the main function tests the positive_or_negeti function
* if it checks for the sign of and interger correctely and returns the
* correct value
* Return: 0
*/

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
