#include "main.h"

/**
 * _isdigit - checks if the character is a digit
 * @c: int to be checked
 * Return: 1 if c is a digit and 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
